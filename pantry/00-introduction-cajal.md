# CAJAL Figure Intelligence — Introduction

**Source:** `chapters/00-introduction.md`
**Mode:** `/scan silent`
**Domain note:** Layout-purpose palette in effect. CS/OOP textbook conventions apply — UML-like class shapes (rectangles), arrows for relationships, no decorative iconography.

---

## Density Recommendation

**1 figure, Foundational density.** The introduction is the book's roadmap and central argument. One book-map figure that traces the seven *Fundamental Themes* across the fourteen modules earns its place — every other figure in the introduction would pre-empt content the modules themselves carry.

---

## Zone Map

- **MC:** Phase gates for AI use — boundary shifts as student verification ability grows.
- **VG:** Book architecture — fourteen modules + appendices, with the seven *Fundamental Themes* recurring across the sequence.
- **PQ:** None.

---

## Figure 0.1 — Book Map: Themes Across Modules

**Priority: Important.** VG. The introduction promises a recurring set of themes across all modules; the reader needs to see the structure.

### Block 1 — Illustrae paste block

A grid composition. Horizontal axis: fourteen module columns labeled implicitly (Module 1 through Module 14) plus four appendix columns at the right (Module 0 Welcome, Midterm Module 7, Claude Code appendix, Fundamental Themes appendix) for completeness. Vertical axis: seven horizontal lanes representing the seven Fundamental Themes (verification, responsibility, debugging-as-causal-reasoning, AI phase gates, project-as-one-system, tests-as-evidence, defense-as-the-point). Each cell is a small filled circle if that theme appears prominently in that module, an outlined-only circle if it appears as background, or empty if it does not appear. Filled circles in Blue `#0072B2`. Outlined circles in Sky Blue `#56B4E9` 1pt outline only. Thin Black `#000000` 0.5pt grid rules separate rows and columns. The grid produces a visible pattern — themes that are dense early (verification, debugging) vs. themes that intensify late (defense-as-the-point, project-as-one-system). White background, flat vector, double-column 174mm preferred.

### Block 2 — Full SCOPE prompt

[S] Double-column 174mm preferred, vector, white background.
[C] Grid showing 7 theme rows × ~18 module/appendix columns. Each cell either filled circle (theme prominent), outlined circle (background), or empty.
[O] Strict grid layout. Theme rows ordered as in the introduction's list. Module columns left-to-right in chapter order.
[P] Filled circles Blue `#0072B2`. Outlined circles Sky Blue `#56B4E9` 1pt outline only. Grid rules Black `#000000` 0.5pt.
[E] No text labels in image, no theme names, no module names, no Java logos, no IDE iconography, no decorative ornament, no shadows.

### Block 3 — Negative prompt

text labels, words, theme names, module names, Java logos, IDE iconography, NetBeans branding, decorative ornament, photographic elements, realistic 3D textures, drop shadows, gradient fills, gradient backgrounds, hand-drawn styles, sketch lines, decorative borders, colorful backgrounds, visual clutter, overlapping unaligned paths, fuzzy borders, watermarks, red-green color combinations, rainbow color scales, 3D perspective distortion

---

## Video Candidate Pass

**FIGURE 0.1 (Book map):** STATIC SUFFICIENT. Architectural overview, not transitional.

**Video candidates identified: 0.**

---

## Split-point note

The seven Fundamental Themes are unpacked individually in their dedicated appendix (Module 97). Visualizing each theme separately here would pre-empt that appendix. One overview figure is enough.
